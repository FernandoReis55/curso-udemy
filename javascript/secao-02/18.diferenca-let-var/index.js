/* Dados Primitivos
string, number, undefined, null, boolean.*/

const nome = 'Fernando'; // string
const nome1  = "Fernando"; //string
const nome3 = `Fernando`; // string
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // undefined -> não aponta para local nenhuma na memória
const sobrenomeAluno = null; // não aponta para local nenhuma na memória
const aprovado = false; // Boolean = true, false (lógico)

let a = 2;
const b = a;
console.log(a, b);

a = 3;
console.log(a, b);

